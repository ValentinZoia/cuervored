export interface MatchesData {
    matchesFiltered: [
      {
        date: string;
        round: string;
        homeOrAway: string;
        opponent: string;
        result: string;
      }
    ];
  }